[package]
name = "forge-tick"
version = "0.1.0"
edition = "2021"
description = "Tile-to-Tick conversion for Plato agent rooms"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
